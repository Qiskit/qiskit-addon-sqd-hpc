---
# Inherit the root configuration and disable checks that are inapplicable to the
# benchmark suite (see test/.clang-tidy for rationale).
InheritParentConfig: true
Checks: '-bugprone-random-generator-seed,-bugprone-throwing-static-initialization'
